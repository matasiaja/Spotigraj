{
  "name": "spotigraj",
  "version": "1.0.0",
  "private": true,
  "description": "Spotigraj — gdzie muzyka łączy ludzi",
  "scripts": {
    "sync-web": "mkdir -p www && cp index.html manifest.json icon-192.png icon-512.png sw.js new.png landing.html 404.html polityka-prywatnosci.html regulamin.html www/",
    "cap:sync": "npm run sync-web && cap sync",
    "cap:ios": "npm run cap:sync && cap open ios",
    "cap:android": "npm run cap:sync && cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
